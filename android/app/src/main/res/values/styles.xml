<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
      실행 화면 테마.
      Capacitor의 기본 흰색 splash 대신 Wavey의 크림 아이스 색을 깔아 두어,
      웹뷰가 뜨기 전 흰 화면이 번쩍이지 않게 한다.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- 웹 화면이 뜨기 전까지 보이는 스플래시 그림 -->
        <item name="android:background">@drawable/splash</item>

        <!-- 안드로이드 12부터는 시스템이 먼저 제 스플래시를 띄운다.
             거기에는 그림 전체가 아니라 가운데 아이콘만 들어간다. -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>

        <!-- 배경이 밝으므로 상태바 아이콘은 어둡게 -->
        <item name="android:statusBarColor">@color/splashBackground</item>
        <item name="android:navigationBarColor">@color/splashBackground</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>
</resources>
